<%= render_index_for WilsonCmsJobOffers::JobOffer, @job_offers,
model: :table,
fields: [listing_header('#', {}),
  listing_header(t('.listing.name'), {}),
  listing_header(t('.listing.summary'), {}),
  ] %>
